StackYields is an open-source discovery layer for Stacks-native stacking and sBTC yield opportunities.

The current MVP validates the backend architecture using live data from the Hiro API. Future milestones include protocol adapters, normalized yield data, and embeddable widgets.
